import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  bottomSheetContent: {
    gap: 8,
  },
  languageTextContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 16,
  },
  languageText: {
    fontSize: 14,
    fontWeight: '600',
  },
});
